# 代码问题清单

> 分析范围：`sorting/` 目录下的三个排序算法程序（`bubble-sort.js`、`quick-sort.js`、`insertion-sort.js`）。

## 问题列表

### 1. 快速排序非原地、额外空间占用高 [严重]

- **位置**：`sorting/quick-sort.js`
- **描述**：每次递归都用 `left`/`right` 两个新数组，并用展开运算 `[...]` 拼接结果，整体额外空间达到 O(n log n)。大数据量时会产生大量内存分配与 GC 压力，偏离了快速排序「原地排序」的核心优势。
- **修正**：改为原地分区（Lomuto 划分），空间降为 O(log n)。

### 2. 快速排序在有序输入下退化为 O(n²) [严重]

- **位置**：`sorting/quick-sort.js`
- **描述**：固定选最后一个元素作基准，遇到已排序或逆序数组（实际中很常见的输入）时，每次划分只能削掉一个元素，复杂度退化到最坏 O(n²)。
- **修正**：随机化基准；并「先递归较小分区、较大分区用循环继续」，把递归深度控制在 O(log n)。

### 3. 缺少模块导出，无法复用与测试 [中等]

- **位置**：三个文件均存在
- **描述**：排序函数没有 `module.exports`，既不能被其他代码 `require`，也无法被测试脚本调用，只能靠人肉看控制台输出判断正确性。
- **修正**：各文件导出排序函数，并用 `require.main === module` 保护演示代码，脚本仍可直接 `node` 运行。

### 4. 缺少输入校验 [低]

- **位置**：三个文件均存在
- **描述**：传入非数组（如 `null`、`undefined`、字符串）时会在运行中途抛出含义模糊的错误，不易定位。
- **修正**：函数开头用 `Array.isArray` 校验，抛出明确的 `TypeError`。

### 5. 缺少自动化测试 [中等]

- **位置**：整个 `sorting/` 目录
- **描述**：三个实现没有任何测试，改动后无法自动验证正确性。
- **修正**：新增 `sorting/sort.test.js`，用 Node 内置 `assert` 覆盖空数组、单元素、已有序、逆序、重复元素、负数等用例，并校验「不修改原数组」。

## 修正后验证

- `node sorting/sort.test.js` 应全部通过；
- 三个脚本各自 `node` 直接运行仍正常输出。
